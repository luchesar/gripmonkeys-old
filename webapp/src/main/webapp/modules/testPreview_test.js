goog.require('goog.testing.jsunit');
goog.require('cursoconducir.TestPreviewModule');

var testBasicTestFeatures = function() {
    fail("first test is failing");
};

var testEmpty = function() {
    fail("first test is failing");
};
